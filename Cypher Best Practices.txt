1. Use node labels whenever possible
For example: Instead of 
	MATCH (t)<-[:TAGGED]-(i)
	RETURN count(t)
Do
	MATCH (t:Tag)<-[:TAGGED]-(i:Item)
	RETURN count(t)
	
2. Force index usage

3. If you only need x results, apply the limit as soon as possible
For example: Instead of
